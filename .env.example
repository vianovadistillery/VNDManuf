# Database
DB_DATABASE_URL=sqlite:///./tpmanuf.db

# API
API_HOST=127.0.0.1
API_PORT=8000

# Nova University LLM — prefer config/openai.json (see config/openai.json.example)
# OPENAI_API_KEY is only used as a fallback if not set in the JSON file.
