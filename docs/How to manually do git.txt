# 1) See what’s unstaged
git status

# 2) Stage EVERYTHING (so pre-commit won’t stash)
git add -A

# 3) Run all hooks explicitly (they will change files if needed)
pre-commit run --all-files

# 4) Stage the auto-fixes the hooks just made
git add -A

# 5) Commit for real
git commit -m "work order progress 1"

git push




Your commit got blocked by commitizen. Everything else passed; now you just need a Conventional Commit–style message that matches the pattern:

(build|bump|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\(\S+\))?!?: <summary>

Quick fix — do one of these

Option A: Commit with a proper message

git commit -m "feat(work-orders): add WO flow and fix formatting"


Other valid examples

git commit -m "refactor(db): normalize line endings and EOF fixes"
git commit -m "docs(vndmanuf): add WO spec and current build status"
git commit -m "chore(pre-commit): apply ruff-format and end-of-file-fixer changes"


Option B: Use Commitizen’s interactive prompt

cz commit
# or
git cz


(Commitizen will ask questions and build a valid message.)
